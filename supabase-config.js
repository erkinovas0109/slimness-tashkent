// TODO: replace with your own Supabase project values (Project Settings -> API).
// The anon/publishable key is safe to expose in client-side code as long as
// Row Level Security policies on the tables restrict what it can do.
window.SLIMNESS_SUPABASE_CONFIG = {
  url: "https://YOUR-PROJECT-REF.supabase.co",
  anonKey: "YOUR-ANON-PUBLIC-KEY",
};
